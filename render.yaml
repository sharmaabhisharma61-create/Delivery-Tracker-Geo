services:
  - type: web
    name: geo-delivery-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      # Supabase Postgres connection string (Session pooler URI) - set in the Render dashboard
      - key: DATABASE_URL
        sync: false
